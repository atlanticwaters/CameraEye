# Product Images in Style Pills - Visual Data Flow

## Complete Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                      pip-datasets.json                               │
│  {                                                                   │
│    "pipDatasets": [                                                  │
│      {                                                               │
│        "productId": "LG-FRIDGE-001",                                │
│        "breadcrumbs": [                                              │
│          { "label": "Appliances" },                                  │
│          { "label": "Refrigerators" },                               │
│          { "label": "French Door Refrigerators" } ← MATCH HERE      │
│        ],                                                            │
│        "media": {                                                    │
│          "primaryImage": "https://images.../fridge.jpg" ← EXTRACT   │
│        }                                                             │
│      }                                                               │
│    ]                                                                 │
│  }                                                                   │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            │ Loaded by
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                     PLPDataLoader.shared                             │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  loadPIPDatasets() → [PIPDataset]                             │ │
│  │  • Decodes JSON                                               │ │
│  │  • Returns array of all products                              │ │
│  └───────────────────────────────────────────────────────────────┘ │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            │ Used by
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│            PLPCategory.withProductImages()                           │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  1. Load datasets                                             │ │
│  │  2. For each style pill config:                               │ │
│  │     • Search for product matching breadcrumb                  │ │
│  │     • Extract media.primaryImage URL                          │ │
│  │     • Create DSStylePillItem with imageURL                    │ │
│  │  3. Return configured PLPCategory                             │ │
│  └───────────────────────────────────────────────────────────────┘ │
│                                                                      │
│  Example Input:                                                      │
│  ┌────────────────────────────────────────────────────────────────┐│
│  │ stylePillConfigs: [                                            ││
│  │   ("French Door\nRefrigerators",                               ││
│  │    "French Door",              ← Breadcrumb match string       ││
│  │    "refrigerator.fill")        ← SF Symbol fallback            ││
│  │ ]                                                              ││
│  └────────────────────────────────────────────────────────────────┘│
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            │ Creates
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                       DSStylePillItem                                │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  text: "French Door\nRefrigerators"                           │ │
│  │  image: Image(systemName: "refrigerator.fill")  ← Fallback    │ │
│  │  imageURL: "https://images.../fridge.jpg"      ← From dataset │ │
│  └───────────────────────────────────────────────────────────────┘ │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            │ Rendered by
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    DSPlpFilterPanel                                  │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │  stylePillView(_ item: DSStylePillItem)                       │ │
│  │  {                                                            │ │
│  │    if let imageURL = item.imageURL {                         │ │
│  │      AsyncImage(url: URL(string: imageURL)) {               │ │
│  │        phase in                                               │ │
│  │        switch phase {                                         │ │
│  │          case .empty:                                         │ │
│  │            ProgressView()         ← Loading state             │ │
│  │          case .success(let image):                            │ │
│  │            image.resizable()...   ← Real product image!       │ │
│  │          case .failure:                                       │ │
│  │            Image(systemName: "photo") ← Error fallback        │ │
│  │        }                                                      │ │
│  │      }                                                         │ │
│  │    } else {                                                   │ │
│  │      item.image                   ← SF Symbol fallback        │ │
│  │    }                                                          │ │
│  │  }                                                            │ │
│  └───────────────────────────────────────────────────────────────┘ │
└───────────────────────────┬─────────────────────────────────────────┘
                            │
                            │ Displayed in
                            ▼
┌─────────────────────────────────────────────────────────────────────┐
│                         PLPView                                      │
│  ┌───────────────────────────────────────────────────────────────┐ │
│  │                                                               │ │
│  │  DSPlpFilterPanel(                                           │ │
│  │    stylePills: category.stylePills  ← With real images!     │ │
│  │  )                                                            │ │
│  │                                                               │ │
│  │  [🖼️ French Door]  [🖼️ Side by Side]  [🖼️ Top Freezer]    │ │
│  │   Real product      Real product       Real product          │ │
│  │   photo shows!      photo shows!       photo shows!          │ │
│  │                                                               │ │
│  └───────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────────┘
```

## State Flow: Loading to Display

```
Initial State (Empty)
├── Style pill renders
├── Checks item.imageURL
└── Finds URL present
         ↓
Loading State
├── AsyncImage starts fetch
├── Shows ProgressView
└── User sees loading indicator
         ↓
Success State  
├── Image data received
├── AsyncImage renders image
└── User sees real product photo! ✅
         
OR

Failure State
├── Network error or invalid URL
├── AsyncImage renders fallback
└── User sees SF Symbol icon
```

## Breadcrumb Matching Logic

```
Given product in dataset:
{
  "breadcrumbs": [
    { "label": "Appliances" },
    { "label": "Refrigerators" },
    { "label": "French Door Refrigerators" }
  ]
}

And style pill config:
("French Door\nRefrigerators", "French Door", "refrigerator.fill")
                                    ↑
                            Breadcrumb match string

Matching Process:
1. Loop through all datasets
2. For each dataset, check breadcrumbs array
3. Check if any breadcrumb.label contains "French Door"
4. "French Door Refrigerators".contains("French Door") → TRUE ✅
5. This product is selected!
6. Extract product.media.primaryImage
7. Use URL in DSStylePillItem
```

## Example: Complete Flow

```
User Opens PLPView
        ↓
PLPView(category: .refrigeratorsWithImages)
        ↓
Category created via withProductImages()
        ↓
┌─────────────────────────────────────────┐
│ 1. Load pip-datasets.json               │
│    → 500 products loaded                │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 2. Process style pill configs           │
│    Config: ("French Door", "French...") │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 3. Search for matching product          │
│    → Found: LG French Door Fridge       │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 4. Extract image URL                    │
│    → "https://images.../lg-fridge.jpg"  │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 5. Create DSStylePillItem               │
│    text: "French Door\nRefrigerators"   │
│    imageURL: "https://images.../..."    │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 6. Render in DSPlpFilterPanel           │
│    → AsyncImage loads from URL          │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 7. Display to user                      │
│    → Real product photo in style pill!  │
│    🎉 Success!                          │
└─────────────────────────────────────────┘
```

## Fallback Chain

```
Primary: Remote Image URL
    ↓ (if URL provided and valid)
AsyncImage loads from internet
    ↓ (if succeeds)
✅ DISPLAYS: Real product photo
    
    ↓ (if fails)
    
Fallback 1: SF Symbol
    ↓ (from config.fallbackIcon)
✅ DISPLAYS: Icon from SF Symbols

    ↓ (if no SF Symbol)
    
Fallback 2: Generic Icon  
✅ DISPLAYS: Generic "photo" icon
```

## Performance Characteristics

```
┌────────────────────────────────────────────────┐
│ Initial Category Creation                      │
│ Time: ~100-200ms                               │
│ • Load JSON                                    │
│ • Parse datasets                               │
│ • Search for matching products                 │
│ • Create style pill items                      │
└────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────┐
│ First Image Load (per pill)                   │
│ Time: ~500-1000ms (network dependent)         │
│ • DNS lookup                                   │
│ • HTTP request                                 │
│ • Image download                               │
│ • Image decode                                 │
└────────────────────────────────────────────────┘
                    ↓
┌────────────────────────────────────────────────┐
│ Subsequent Loads (cached)                     │
│ Time: ~10-50ms                                 │
│ • Cache hit                                    │
│ • Image decode (if needed)                     │
│ • Display                                      │
└────────────────────────────────────────────────┘
```

## Memory Management

```
AsyncImage Lifecycle:
┌─────────────────────────────────────────┐
│ 1. Image appears on screen              │
│    → Allocates memory for image         │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 2. User scrolls away                    │
│    → View removed from hierarchy        │
│    → AsyncImage releases memory         │
└─────────────────┬───────────────────────┘
                  ↓
┌─────────────────────────────────────────┐
│ 3. User scrolls back                    │
│    → Cache hit (if still cached)        │
│    → Quick redisplay                    │
└─────────────────────────────────────────┘

Cache Strategy:
• URLCache manages automatic caching
• Default cache size: ~512 MB memory, ~10 GB disk
• Automatic eviction when full
• Respects HTTP cache headers
```

## Error Handling

```
Request Started
      ↓
  [Success?]
      ├─ YES → Display image ✅
      │
      └─ NO
          ↓
    [Network Error?]
          ├─ YES → Show SF Symbol
          │         Log: "Failed to load image"
          │
          └─ NO
              ↓
        [Invalid URL?]
              ├─ YES → Show SF Symbol
              │         Log: "Invalid image URL"
              │
              └─ NO
                  ↓
            [Decode Error?]
                  └─ YES → Show SF Symbol
                            Log: "Failed to decode image"
```

## Comparison: Before vs After

### Before (SF Symbols Only)
```
PLPCategory.refrigerators
        ↓
    stylePills: [
        DSStylePillItem(
            text: "French Door",
            image: Image(systemName: "refrigerator.fill")
        )
    ]
        ↓
    Renders generic icon
    🔴 Not representative of actual products
```

### After (Real Product Images)
```
PLPCategory.refrigeratorsWithImages
        ↓
    withProductImages() factory method
        ↓
    Searches dataset for "French Door"
        ↓
    Finds matching product
        ↓
    Extracts image URL
        ↓
    stylePills: [
        DSStylePillItem(
            text: "French Door",
            image: Image(systemName: "refrigerator.fill"),  ← Fallback
            imageURL: "https://images.../real-fridge.jpg"   ← Real image!
        )
    ]
        ↓
    AsyncImage loads from URL
        ↓
    Renders real product photo
    ✅ Shows actual product imagery!
```

---

## Summary

This architecture provides:
- ✅ **Automatic** image discovery from dataset
- ✅ **Robust** fallback handling
- ✅ **Performant** async loading with caching
- ✅ **Flexible** supports both local and remote images
- ✅ **Maintainable** single source of truth (pip-datasets.json)

The result is a professional filter panel with real product photography! 🎉
